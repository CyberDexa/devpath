---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/layout/Header.astro";
import ActivityFeed from "../components/social/ActivityFeed";
---

<BaseLayout title="Activity Feed | SkillRoute">
  <Header />

  <main class="pt-20 min-h-screen">
    <div class="max-w-3xl mx-auto px-6 py-8">
      {/* Header */}
      <div class="mb-8">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-10 h-10 rounded-xl bg-violet/10 border border-violet/20 flex items-center justify-center">
            <svg class="w-5 h-5 text-violet" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
              <circle cx="9" cy="7" r="4" />
              <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
              <path d="M16 3.13a4 4 0 0 1 0 7.75" />
            </svg>
          </div>
          <div>
            <h1 class="text-2xl md:text-3xl font-display font-bold text-bright">Activity Feed</h1>
            <p class="text-sm text-dim">See what the community is building, learning, and achieving</p>
          </div>
        </div>

        <div class="flex flex-wrap gap-2 mt-4">
          <span class="px-3 py-1 rounded-full text-xs bg-violet/8 text-violet border border-violet/15">
            👥 Community
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-teal/8 text-teal border border-teal/15">
            🎯 Achievements
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-amber/8 text-amber border border-amber/15">
            🔥 Streaks
          </span>
        </div>
      </div>

      <ActivityFeed client:load />
    </div>
  </main>
</BaseLayout>
