---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/layout/Header.astro";
import Playground from "../components/playground/Playground";
---

<BaseLayout title="Code Playground | SkillRoute">
  <Header />

  <main class="pt-20 min-h-screen">
    <div class="max-w-7xl mx-auto px-6 py-8">
      {/* Header */}
      <div class="mb-8">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-10 h-10 rounded-xl bg-teal/10 border border-teal/20 flex items-center justify-center">
            <svg class="w-5 h-5 text-teal" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <polyline points="16 18 22 12 16 6" />
              <polyline points="8 6 2 12 8 18" />
            </svg>
          </div>
          <div>
            <h1 class="text-2xl md:text-3xl font-display font-bold text-bright">Code Playground</h1>
            <p class="text-sm text-dim">Write, run, and test code in your browser — JavaScript, TypeScript, Python, and HTML</p>
          </div>
        </div>

        {/* Feature pills */}
        <div class="flex flex-wrap gap-2 mt-4">
          <span class="px-3 py-1 rounded-full text-xs bg-teal/8 text-teal border border-teal/15">
            ✨ Syntax Highlighting
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-violet/8 text-violet border border-violet/15">
            ▶ Real Execution
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-amber/8 text-amber border border-amber/15">
            🤖 AI Code Review
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-rose/8 text-rose border border-rose/15">
            📸 Version History
          </span>
        </div>
      </div>

      <Playground client:only="react" />
    </div>
  </main>
</BaseLayout>
