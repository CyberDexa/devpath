---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/layout/Header.astro";
import CodeBattles from "../components/battles/CodeBattles";
---

<BaseLayout title="Code Battles | SkillRoute">
  <Header />

  <main class="pt-20 min-h-screen">
    <div class="max-w-4xl mx-auto px-6 py-8">
      {/* Header */}
      <div class="mb-8">
        <div class="flex items-center gap-3 mb-3">
          <div class="w-10 h-10 rounded-xl bg-rose/10 border border-rose/20 flex items-center justify-center">
            <svg class="w-5 h-5 text-rose" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path d="M14.5 17.5L3 6V3h3l11.5 11.5" />
              <path d="M13 19l6-6" />
              <path d="M16 16l4 4" />
              <path d="M19 21l2-2" />
            </svg>
          </div>
          <div>
            <h1 class="text-2xl md:text-3xl font-display font-bold text-bright">Code Battles</h1>
            <p class="text-sm text-dim">Compete 1v1 in timed coding challenges — test your speed and skill</p>
          </div>
        </div>

        <div class="flex flex-wrap gap-2 mt-4">
          <span class="px-3 py-1 rounded-full text-xs bg-rose/8 text-rose border border-rose/15">
            ⚔️ 1v1 Battles
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-amber/8 text-amber border border-amber/15">
            ⏱ Timed Challenges
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-teal/8 text-teal border border-teal/15">
            🏆 XP Rewards
          </span>
          <span class="px-3 py-1 rounded-full text-xs bg-violet/8 text-violet border border-violet/15">
            📊 Rankings
          </span>
        </div>
      </div>

      <CodeBattles client:load />
    </div>
  </main>
</BaseLayout>
